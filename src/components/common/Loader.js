import React from 'react';
import {FoldingCube} from 'styled-spinkit';

export const Loader = () => {
    return(<FoldingCube color="papayawhip"/>
    );
}

export const BoxLoader = () => {
    return(<FoldingCube size="15" color="darkseagreen"/>
    );
}