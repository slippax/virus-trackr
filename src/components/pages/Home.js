import React from 'react'
import {Api} from '../../api/api';
import {Header} from '../common';
const Home = () => {
    return (
        <div>
            <Header/>
            <Api/>
        </div>
    )
}

export default Home
